{
  "extends": "next/core-web-vitals",
  "rules": {
    "no-console": ["error", { "allow": [] }]
  },
  "overrides": [
    {
      "files": ["src/lib/logger.ts", "scripts/**/*.ts", "prisma/seed.ts"],
      "rules": { "no-console": "off" }
    }
  ]
}
